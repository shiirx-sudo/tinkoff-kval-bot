# T-Invest API — актуальный SDK
tinkoff-investments==0.2.0b65

# Работа с датами
python-dateutil==2.9.0

# Часовые пояса (нужно для Windows и некоторых Linux-окружений)
tzdata==2024.1

# Переменные окружения
python-dotenv==1.0.1

# Логирование
loguru==0.7.2

# Форматированный вывод в консоль
rich==13.7.0

# Типизация и утилиты (входит в stdlib 3.10+, но явно указываем)
# dataclasses — stdlib
# decimal    — stdlib
# csv        — stdlib
# json       — stdlib
