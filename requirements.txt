# T-Invest Invest API — обращение по REST напрямую (без SDK).
# Пины заданы НИЖНИМИ границами (>=), чтобы установка не откатывала уже
# установленные более свежие версии (важно при глобальной установке без venv).

requests>=2.31           # HTTP к invest-public-api.tinkoff.ru/rest
python-dateutil>=2.9     # работа с датами
tzdata>=2024.1           # часовые пояса (Windows и часть Linux)
python-dotenv>=1.0.1     # переменные окружения из .env
loguru>=0.7.2            # логирование
rich>=13.7.0             # форматированный вывод в консоль
